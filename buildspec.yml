version: 0.2

phases:
  pre_build:
    commands:
      - echo "Pre-build phase"
  build:
    commands:
      - echo "Build phase"
      - docker run -v $CODEBUILD_SRC_DIR:/app maven:latest ls
  post_build:
    commands:
      - echo "Post-build phase"

artifacts:
  files: '**/target/*.jar'
